language: cpp

os:
  - linux

compiler:
  - clang

stages:
  - configure
  - build
  - test

jobs:
  include:
    - stage: Configure
      script: mkdir build && cd build && cmake ..
    - stage: Build
      script: make
    - stage: Tests
      script: ./tests
